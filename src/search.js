const search = {
    '>': (a, b) => (a > b),
    '>=': (a, b) => (a >= b),
    '<': (a, b) => (a < b),
    '<=': (a, b) => (a <= b),
    '!=': (a, b) => (a != b),
    '==': (a, b) => (a == b),
}

export default search